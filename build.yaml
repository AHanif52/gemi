# Kunci JSON = nama kolom SQL (initial_balance, bukan initialBalance),
# supaya file backup memakai nama field yang sama dengan model data di BRD.
targets:
  $default:
    builders:
      drift_dev:
        options:
          use_sql_column_name_as_json_key: true
